A simple tool to manage an empty network namespace for lrun. (with lo dev up)

    lrun-netns-empty-lo create   # equivalent to "ip netns add lrun-empty-lo"
    lrun-netns-empty-lo destroy  # equivalent to "ip netns del lrun-empty-lo"
    lrun-netns-empty-lo status   # show status of the namespace
    lrun-netns-empty-lo help     # show help
